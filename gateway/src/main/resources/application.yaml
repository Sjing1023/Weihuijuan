

spring:
  application:
    name: gateway
  config:
    import: nacos:gateway-dev.yaml
  cloud:
    #nacos注册中心
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml







